
* Measure JNW_GR03
.control

load {cicname}.raw

echo "MEAS_START"

foreach vtemp -25 0 25 50 75 100 120

    echo "LINE_SHIFT"
    echo "LINE_SHIFT"
    set fend = .raw
    load {cicname}_$vtemp$fend

    meas tran OUT$vtemp find v(OUT) at=10u
    meas tran COMP$vtemp find v(COMP) at=10u
    
end

echo "MEAS_END"
.endc
